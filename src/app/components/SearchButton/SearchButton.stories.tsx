import React from 'react';

import SearchButton from './SearchButton';

export default {
  title: 'Component/SearchButton',
  component: SearchButton,
};

export const Search = (): JSX.Element => <SearchButton>Search</SearchButton>;
